@model ShoesWebApplication.Models.Good

@{
    ViewData["Title"] = "Добавление товара";
}

<h2 class="mb-4">Добавление товара</h2>

<div class="row">
    <div class="col-md-6">

        <form asp-action="Create" enctype="multipart/form-data">

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Name" class="form-label">Название</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label">Описание</label>
                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="row">

                <div class="col-md-6 mb-3">
                    <label asp-for="Price" class="form-label">Цена</label>
                    <input asp-for="Price" type="number" lang="en" step="0.01" class="form-control" inputmode="decimal" />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>

                <div class="col-md-6 mb-3">
                    <label asp-for="Discount" class="form-label">Скидка (%)</label>
                    <input asp-for="Discount" class="form-control" />
                    <span asp-validation-for="Discount" class="text-danger"></span>
                </div>

            </div>

            <div class="mb-3">
                <label asp-for="Count" class="form-label">Количество на складе</label>
                <input asp-for="Count" class="form-control" />
                <span asp-validation-for="Count" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label class="form-label">Изображение</label>
                <input type="file"
                       name="imageFile"
                       class="form-control"
                       accept=".jpg,.jpeg,.png" />
            </div>

            <div class="mb-3">
                <label asp-for="GoodCategoryId" class="form-label">Категория</label>
                <select asp-for="GoodCategoryId"
                        asp-items="ViewBag.Categories"
                        class="form-select">
                </select>
            </div>

            <div class="mb-3">
                <label asp-for="ManufacturerId" class="form-label">Производитель</label>
                <select asp-for="ManufacturerId"
                        asp-items="ViewBag.Manufacturers"
                        class="form-select">
                </select>
            </div>

            <div class="mb-3">
                <label asp-for="SupplierId" class="form-label">Поставщик</label>
                <select asp-for="SupplierId"
                        asp-items="ViewBag.Suppliers"
                        class="form-select">
                </select>
            </div>

            <div class="mb-3">
                <label asp-for="GoodMeasurementId" class="form-label">Единица измерения</label>
                <select asp-for="GoodMeasurementId"
                        asp-items="ViewBag.Measurements"
                        class="form-select">
                </select>
            </div>

            <button type="submit" class="btn btn-primary">
                Сохранить
            </button>

            <a asp-action="Index" class="btn btn-secondary">
                Назад
            </a>

        </form>

    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}